@model RobotsTxt
@{
    var userProfile = ContextProvider.CurrentUserProfile;
    if (userProfile == null || !(userProfile.IsContentApprover || userProfile.IsContentEditor))
    {
        Layout = null;
    }
    else
    {
        <text>
            <style>
                body {
                    background-color: white;
                    margin: 20px;
                    white-space: pre;
                }
            </style>
        </text>
    }
    Response.Write(Model.Text);
}

@foreach (var sitemap in Model.Sitemaps)
{
    @* Using Response.Write instead of Razor due to extra whitespace being added if this is not done *@
    Response.Write("\n\nsitemap: " + sitemap);
}